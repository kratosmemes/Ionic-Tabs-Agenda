import { FechastringPipe } from './fechastring.pipe';

describe('FechastringPipe', () => {
  it('create an instance', () => {
    const pipe = new FechastringPipe();
    expect(pipe).toBeTruthy();
  });
});
